# Dataset Workspace

Source manifests and metadata are stored in `step1_base/` and
`step2_new_classes/`. Images, labels, COCO80 materializations, incremental
mixtures, and replay memory are generated locally and excluded from Git.

See [`docs/dataset.md`](../docs/dataset.md) for the complete curation protocol,
class mapping, subset sizes, and rebuild commands.
